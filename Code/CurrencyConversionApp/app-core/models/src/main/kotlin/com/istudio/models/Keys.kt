package com.istudio.models

import com.istudio.models.local.CurrencyEntity

object Keys {

    val defaultCurrency = CurrencyEntity(
        currencyKey = "USD", currencyName = "United States Dollar"
    )

}